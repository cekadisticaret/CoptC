workflows:
  ios-testflight:
    name: CoptC iOS TestFlight
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: CoptC
    environment:
      groups:
        - ios_signing
      vars:
        BUNDLE_ID: tr.deadella.coptc
        XCODE_SCHEME: CoptC
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Install XcodeGen
        script: |
          brew install xcodegen
      - name: Generate Xcode project
        script: |
          cd ios
          # Stale DerivedData temizle — önbellekten kalan eski .o dosyaları derleme hatalarına neden oluyor
          rm -rf ~/Library/Developer/Xcode/DerivedData/CoptC-*
          BUILD_VER="$(date +%s)"
          echo "Using version 1.0.21 ($BUILD_VER)"
          xcodegen generate
          sed -i '' "s/CURRENT_PROJECT_VERSION = .*/CURRENT_PROJECT_VERSION = ${BUILD_VER};/g" CoptC.xcodeproj/project.pbxproj
          sed -i '' "s/MARKETING_VERSION = .*/MARKETING_VERSION = 1.0.21;/g" CoptC.xcodeproj/project.pbxproj
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.0.21" CoptC/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_VER" CoptC/Info.plist
          echo "plist version=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' CoptC/Info.plist) build=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' CoptC/Info.plist)"
      - name: Set up keychain
        script: keychain initialize
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create \
            --certificate-key @env:CERTIFICATE_PRIVATE_KEY
      - name: Add certificates to keychain
        script: keychain add-certificates
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Build and export IPA
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/ios/CoptC.xcodeproj" \
            --scheme "$XCODE_SCHEME"
      - name: Upload to TestFlight
        script: |
          IPA=$(find "$CM_BUILD_DIR" -name "*.ipa" | head -1)
          echo "IPA: $IPA"
          app-store-connect publish --path "$IPA"
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - cekadisticaret@gmail.com
        notify:
          success: true
          failure: true
