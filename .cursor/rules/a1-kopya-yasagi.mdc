---
description: Kaynak 0.40 fiyat tabanı yok sayılır
alwaysApply: true
---

# Taban follow (2026-08-27)

Kullanıcı: “bu engeli kaldır” — `entry_price_min` 0.40 kopyayı kesmesin.

**Ne değişti**
- `poly/coptc_mirror.py`: `copyable=false` yalnız tabandan geliyorsa yine kopyala.
- `order_guards`: `min_price=0.02` (PM tabanı). Tavan / max_spend kaynaktan.
- A2 ve TOP3 aynı: yaş / pencere / copyable (taban hariç) duruyor.

**Geri al:** taban kontrolünü geri koy (`copyable` + `entry_price_min`).
